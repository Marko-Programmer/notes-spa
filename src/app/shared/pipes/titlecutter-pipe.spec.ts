import { TitlecutterPipe } from './titlecutter-pipe';

describe('TitlecutterPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecutterPipe();
    expect(pipe).toBeTruthy();
  });
});

